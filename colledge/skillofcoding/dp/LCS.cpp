#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main() {
    int n,k;
    cin >> n >> k;
    string lists[k];
    for(int i = 0;i < k;i++) {
        cin >> lists[i];
    }
    for(int i = 1;i < k;i++) {
        cout << endl;
    }
}